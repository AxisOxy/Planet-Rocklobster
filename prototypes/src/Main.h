#ifndef _MAIN_H
#define _MAIN_H

// declaration of global objects
extern BOOL			quit;
extern Display2D	*display;
extern Gfx			*gfx;
extern Input		*input;
extern Timer		*timer;
extern Effect		*effect;
extern char			apppath[_MAX_PATH];

#endif // _MAIN_H
